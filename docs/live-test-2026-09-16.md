# 真实换装测试：2026-09-16

用户明确授权后，分别使用 `nano-banana-2` 和 `gpt-image-2.5` 生成一次。调用生产代码中的配置加载、图片编码、请求构造与异步客户端；这是后端真实 API 测试，不是前端点击入队测试。

## 输入与参数

相同提示词、相同有序三图，仅模型参数不同。英文提示词和中文译文保存在 `tests/live_case.json`，实际请求副本在本地报告中。

1. 图片1 人物.webp：1500×2000，基础人物与构图。
2. 图片 2 上衣.webp：800×800，卡其绿色系立领束腰上衣参考。
3. 图片 3 裤子.webp：960×914，浅蓝破洞拼接牛仔裤参考。

注意：图片3在首次检查后、实际执行前发生替换。最初看到的800×800黑色裤子并非本次实际提交内容。实际提交文件的 SHA-256 为 `92b0e076ddfd1f70a683f07d74366fa5ae61b46d1a496e4baf9d38e61504b714`。两个模型共用一次加载/编码后的三图，执行间不会重新读取文件。

实际请求主机为 `https://grsai.dakka.com.cn`，路径为 `POST /v1/api/generate`、`GET /v1/api/result`，`replyType=async`，`images` 为三个裸 PNG base64 字符串。

| 模型 | 请求参数 | 实际输出 | 总耗时 | POST / GET 次数 |
| --- | --- | --- | --- | --- |
| nano-banana-2 | aspectRatio=3:4, imageSize=1K | 896×1200，1 张 | 42.963 秒 | 1 / 13 |
| gpt-image-2.5 | aspectRatio=1090x1443, quality=auto | 1086×1449，1 张 | 78.176 秒 | 1 / 24 |

两者均 HTTP 200 提交，经历 running → succeeded，成功下载并解码。未重新提交、未更换编码。实际尺寸未经缩放或补边；GPT 未严格遵循所请求的像素尺寸，不能将请求值当成输出尺寸保证。

任务 ID：

- Nano：`14-893f88ad-46f4-4fe8-a0d6-61934dd7bb6b`
- GPT：`14-b1d77b30-1291-4299-8761-d5407e89ec7e`

## 视觉检查

人工对照参考图的定性观察，不是身份相似度或像素一致性的自动评分：

- 两张均换成参考上衣与拼接牛仔裤，保留了标题、边框、暖色背景、手提包等主要元素，没有复制产品尺寸标注。
- Nano 改变了原图前后交错的腿部姿态，人物占画面的比例/位置也有变化，袖口与拼布细节存在重新解释。不能满足“其他部分完全不变”的严格验收。
- GPT 对原图前后腿位置、人物构图的保留更接近原图；但领口、腰带垂落、褶皱、拼布尺寸/纹理等仍有差异，同样不能宣称服饰细节完全一致。
- 本次只有每个模型一个样本，不代表稳定质量排名。

## 本地结果

目录：`tests/outputs/20260916T141447665937Z/`。

- `report.json`：输入哈希、双语提示词、请求阶段、HTTP 状态、任务 ID、耗时、尺寸及输出哈希。
- `nano-banana-2-1.png`。
- `gpt-image-2.5-1.png`。

两张 PNG 均无附加文本/工作流元数据。Key 经隐藏终端输入传入，不写入代码、报告或 PNG。输入和输出目录均已加入 Git 忽略。没有查询余额，因此本次无法给出单次费用，也没有验证真实余额接口。

复现命令见项目 README。正常 pytest 不调用真实 API；重新运行 `--allow-paid` 会创建新的付费任务。
